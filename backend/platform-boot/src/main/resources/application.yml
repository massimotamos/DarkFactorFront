spring:
  application:
    name: darkfactor-platform
  datasource:
    url: jdbc:postgresql://localhost:5432/darkfactor
    username: darkfactor
    password: darkfactor
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

darkfactor:
  dsl-storage:
    root-path: ./dsl-snapshots
